from django.shortcuts import render
from django.http import HttpResponse

# Create your views here.
def captain(request):
    return HttpResponse('ro is a best betsman')


def captain(request):
    return HttpResponse('rohit is mind blowing')
